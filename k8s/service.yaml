apiVersion: v1
kind: Service
metadata:
  name: soroban-pulse
  labels:
    app: soroban-pulse
spec:
  type: ClusterIP
  selector:
    app: soroban-pulse
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
